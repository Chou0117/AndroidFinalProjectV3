<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.antho.android_final">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="stateHidden">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FoodActivity" />
        <activity android:name=".AutomobileActivity" />
        <activity android:name=".ThermostatActivity" />
        <activity android:name=".ThermostatDetails" />
        <activity android:name=".ActivityActivity" />
        <activity android:name=".Activity_NewRecord" />
        <activity android:name=".Activity_PastRecord" />
        <activity android:name=".FoodDetail" />
        <activity android:name=".Activity_MessageDetails"></activity>
    </application>

</manifest>